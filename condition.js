const prompt = require('prompt-sync')();

//let a =Number(prompt("enter un nombre "));
//if (a % 2 === 0){
    //console .log("le nombre est pair");
// } else {
     //console .log("le nombre na pas pair");
//}


switch(expression) {
  case x:
    // code block
    break;
  case y:
    // code block
    break;
  default:
    // code block
}